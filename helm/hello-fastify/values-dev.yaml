# Dev overrides. Woodpecker CI rewrites image.tag on every build via sed —
# keep that line exactly as `  tag: <value>` so the CI regex keeps matching.
image:
  tag: 80c7d9706cc2c4b2daa663fad93c1fc8851f88ed
