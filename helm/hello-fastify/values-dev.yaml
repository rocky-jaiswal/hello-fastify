# Dev overrides. image.tag is bumped by hand after a Woodpecker CI build —
# the automated git-push-back step doesn't work yet (see PLAN.md Known Gaps
# in the infra repo). Set it to the commit SHA Woodpecker just built/pushed.
image:
  tag: 4c6a7b8da7641af5bfdce2a34e7973c96639adee
